import { registerRootComponent } from 'expo';
import 'react-native-get-random-values';
import App from './app/App';

// Register the root component so Expo can load the app properly
registerRootComponent(App);
