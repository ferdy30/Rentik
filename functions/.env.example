# Firebase Functions .env example for Rentik
# Copy to .env (not committed) and set your real values.

# Stripe secret key (Test or Live)
STRIPE_SECRET=sk_test_xxx

# Stripe webhook signing secret for the configured endpoint
STRIPE_WEBHOOK_SECRET=whsec_xxx
